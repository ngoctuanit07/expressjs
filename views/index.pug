extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/",method="post")
    input(name="lead_email" type="text",placeholder="enter your email")
    button(type="submit") Submit
